package com.retryjournal.sample.app

/** The Android emulator's NAT alias for the host machine's `localhost`. On a physical device,
 * override with your machine's LAN IP instead — see retry-sample/README.md. */
internal actual val platformServerHost: String = "10.0.2.2"
